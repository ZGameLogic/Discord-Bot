apiVersion: v1
kind: Service
metadata:
  name: discord-bot-dev
  namespace: dev
spec:
  selector:
    app: discord-bot-dev
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
